@model List<string>
@{
    ViewData["Title"] = "ErrorPage";
}


<h2>Error Page</h2>

<hr />
<div class="alert alert-danger">
    <ul>
        @foreach (var item in Model)
        {
            <li>@item</li>
        }
    </ul>
</div>